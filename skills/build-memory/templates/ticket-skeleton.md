<!--
  Template: skeleton ticket (BM-TICKET-03). A placeholder for work whose body is written
  only after a named gate. Filled by: decompose-spec. When: seed (after a gate).
  A skeleton has NO Run: line — the validator refuses to run one, and implement-spec stops
  if asked to. It carries only the header, ## Scope (one line), ## Spec §§ and ## REQ coverage.
-->
# <ID> — <short title>

- **Sequence:** <n> of <N> · **Phase:** <phase> · **Kind:** skeleton
- **base_branch:** current checkout
- **Depends on:** <prior ids>
- **Gate status:** none

> Skeleton only — the body is written after **GATE-G<k>**. Do not implement this file until
> then; it carries no run line.

## Scope (one line)
<the one thing this ticket will do once its gate opens>

## Spec §§
<the spec sections it will implement>

## REQ coverage
<the REQ ids it will satisfy>
